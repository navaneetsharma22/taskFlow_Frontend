# TaskFlow SaaS — Frontend Environment Variables
# Expose parameters to the Vite client using VITE_ prefix.

# The REST Endpoint of the TaskFlow Backend API
VITE_API_URL=http://localhost:5000/api

# The socket server base address for collaborative Kanban synchronization
VITE_SOCKET_URL=http://localhost:5000
